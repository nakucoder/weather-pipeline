version: "3.8"

services:
  weather-pipeline:
    build: .
    container_name: miami-weather-pipeline
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped